import * as React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
    <React.StrictMode>Using ESLint and Prettier in a TypeScript</React.StrictMode>,
    document.getElementById('root') as HTMLElement,
);
